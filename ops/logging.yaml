version: 1
formatters:
  json:
    format: '{"time":"%(asctime)s","level":"%(levelname)s","logger":"%(name)s","msg":"%(message)s"}'
handlers:
  console:
    class: logging.StreamHandler
    formatter: json
    level: INFO
root:
  level: INFO
  handlers: [console]
