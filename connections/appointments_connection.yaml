spec_version: v1
kind: connection
name: appointment_scheduler
description: Appointment scheduling backend (stub or real EHR/calendar).
environments:
  draft:
    kind: basic
    type: team
    server_url: ${APPT_API_URL}
    auth:
      scheme: NONE
